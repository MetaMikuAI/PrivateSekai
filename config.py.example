import json
import logging


logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')
logger = logging.getLogger(__name__)

# get key and iv from frida-il2cpp-bridge first
key = b''
iv = b''

jwt_key = "MetaMiku@ProjectSekaiPrivateServerJWT"
jwt_alg = "HS256"

MAGIC_BYTES = bytes.fromhex('****************')
IGNORE_INVALID_CREDENTIAL = True

SKIP_TUTORIAL = False

DEBUG = True

def user_customize_patch(user):
    # example
    user['userChargedCurrency']['paid'] = 20070831

with open('template/api_user_auth.json', 'r', encoding='utf-8') as f:
    api_user_auth = json.load(f)

with open('template/api_system.json', 'r', encoding='utf-8') as f:
    api_system = json.load(f)